import 'package:flutter/material.dart';

class AppColorsPrimary {
  static const primary = Color(0xFF0D47A1);      // Blue-900
  static const primaryLight = Color(0xFF1565C0); // Blue-800
  static const primaryDark = Color(0xFF002171);  // Blue-950
}

// =============================
// Tailwind-Type Color Palettes
// =============================
class AppColorsSecondary {
  static const secondary = Color(0xFF64748B);      // Slate-500
  static const secondaryLight = Color(0xFF94A3B8); // Slate-300
  static const secondaryDark = Color(0xFF334155);  // Slate-700
}

class AppColorsSuccess {
  static const success = Color(0xFF16A34A);      // Green-600
  static const successLight = Color(0xFF4ADE80); // Green-400
  static const successDark = Color(0xFF15803D);  // Green-700
}

class AppColorsDanger {
  static const danger = Color(0xFFDC2626);      // Red-600
  static const dangerLight = Color(0xFFF87171); // Red-400
  static const dangerDark = Color(0xFFB91C1C);  // Red-700
}

class AppColorsWarning {
  static const warning = Color(0xFFF59E0B);      // Amber-500
  static const warningLight = Color(0xFFFACC15); // Amber-400
  static const warningDark = Color(0xFFD97706);  // Amber-600
}

class AppColorsInfo {
  static const info = Color(0xFF0284C7);      // Sky-600
  static const infoLight = Color(0xFF38BDF8); // Sky-400
  static const infoDark = Color(0xFF0369A1);  // Sky-700
}

class AppColorsLight {
  static const light = Color(0xFFF1F5F9);      // Slate-100
  static const light2 = Color(0xFFE2E8F0);     // Slate-200
}

class AppColorsDark {
  static const dark = Color(0xFF0F172A);       // Slate-900
  static const dark2 = Color(0xFF1E293B);      // Slate-800
}

class AppColorsLink {
  static const link = Color(0xFF2563EB);       // Blue-600
  static const linkHover = Color(0xFF1D4ED8);  // Blue-700
}
